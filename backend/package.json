{
  "name": "@archlens/backend",
  "private": true,
  "version": "0.1.0",
  "description": "ArchLens backend — Vercel serverless functions: diagram generation proxy, quota/license gate, Razorpay billing.",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@mermaid-js/layout-elk": "^0.2.3",
    "@supabase/supabase-js": "^2.45.4",
    "mermaid": "^11.14.0",
    "puppeteer-core": "^24.43.0",
    "razorpay": "^2.9.8"
  },
  "devDependencies": {
    "@vercel/node": "^3.2.11",
    "@xmldom/xmldom": "^0.9.12",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
