{
  "name": "@archlens/action",
  "private": true,
  "version": "0.1.0",
  "description": "ArchLens GitHub Action — extracts PR diffs, calls the ArchLens API, posts the resulting architecture diagram as a PR comment.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --noEmit && ncc build src/index.ts -o dist --minify --target es2022",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "minimatch": "^9.0.5"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.45.0",
    "tsup": "^8.2.4",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
